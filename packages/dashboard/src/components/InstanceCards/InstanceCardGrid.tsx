import React from 'react';

interface IInstanceCardGridProps {
  ec2DashApiBaseUrl: string;
}

const InstanceCardGrid: React.FC<IInstanceCardGridProps> = (props) => {
  throw Error();
};

export default InstanceCardGrid;
